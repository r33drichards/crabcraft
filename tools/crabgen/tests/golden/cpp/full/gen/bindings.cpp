// Code generated by crabgen. DO NOT EDIT.
//
// WIRE codec helpers for every named type, the crab_invoke dispatch
// handlers + registration, the typed mesh wrappers, and the embedded
// schema (crab::SchemaJson) for WIT package crab:full@0.1.0, world full.
//
// Error semantics (WIRE.md section 2): a handler maps a non-empty impl
// .err to a status-1 reply "<function>: <message>" — except for functions
// whose WIT result is result<T, string> (or a result with no err payload),
// where the impl's .err becomes the WIRE result ERR CASE: a normal
// status-0 reply carrying an encoded result value.

#include "bindings.hpp"

#include <utility>

#include "mesh.hpp"
#include "schema_json.h"

// Codec helpers are emitted for every named type in both directions even
// when this module only encodes or only decodes it (analog of the Rust
// lane's #![allow(dead_code)]; clang honors GCC diagnostic pragmas).
#pragma GCC diagnostic ignored "-Wunused-function"

namespace gen {
namespace {

// encodeEverything appends the WIRE encoding of v; "" = success.
std::string encodeEverything(std::vector<uint8_t>& out, const Everything& v) {
  crab::EncodeBool(out, v.a_bool);
  crab::EncodeU8(out, v.a_u8);
  crab::EncodeU16(out, v.a_u16);
  crab::EncodeU32(out, v.a_u32);
  crab::EncodeU64(out, v.a_u64);
  crab::EncodeS8(out, v.a_s8);
  crab::EncodeS16(out, v.a_s16);
  crab::EncodeS32(out, v.a_s32);
  crab::EncodeS64(out, v.a_s64);
  crab::EncodeF32(out, v.a_f32);
  crab::EncodeF64(out, v.a_f64);
  crab::EncodeChar(out, v.a_char);
  crab::EncodeString(out, v.a_string);
  crab::EncodeListLen(out, v.a_list.size());
  for (const auto& x0 : v.a_list) {
    crab::EncodeU32(out, x0);
  }
  crab::EncodeOptionTag(out, v.a_opt.has_value());
  if (v.a_opt.has_value()) {
    const auto& x1 = *v.a_opt;
    crab::EncodeString(out, x1);
  }
  crab::EncodeU8(out, std::get<0>(v.a_tuple));
  crab::EncodeString(out, std::get<1>(v.a_tuple));
  crab::EncodeBool(out, std::get<2>(v.a_tuple));
  return std::string();
}

// decodeEverything decodes a Everything off d.
crab::Res<Everything> decodeEverything(crab::Decoder& d) {
  Everything v{};
  auto r0 = d.Bool();
  if (!r0.ok()) return crab::Res<Everything>::fail(std::move(r0.err));
  v.a_bool = std::move(r0.val);
  auto r1 = d.U8();
  if (!r1.ok()) return crab::Res<Everything>::fail(std::move(r1.err));
  v.a_u8 = std::move(r1.val);
  auto r2 = d.U16();
  if (!r2.ok()) return crab::Res<Everything>::fail(std::move(r2.err));
  v.a_u16 = std::move(r2.val);
  auto r3 = d.U32();
  if (!r3.ok()) return crab::Res<Everything>::fail(std::move(r3.err));
  v.a_u32 = std::move(r3.val);
  auto r4 = d.U64();
  if (!r4.ok()) return crab::Res<Everything>::fail(std::move(r4.err));
  v.a_u64 = std::move(r4.val);
  auto r5 = d.S8();
  if (!r5.ok()) return crab::Res<Everything>::fail(std::move(r5.err));
  v.a_s8 = std::move(r5.val);
  auto r6 = d.S16();
  if (!r6.ok()) return crab::Res<Everything>::fail(std::move(r6.err));
  v.a_s16 = std::move(r6.val);
  auto r7 = d.S32();
  if (!r7.ok()) return crab::Res<Everything>::fail(std::move(r7.err));
  v.a_s32 = std::move(r7.val);
  auto r8 = d.S64();
  if (!r8.ok()) return crab::Res<Everything>::fail(std::move(r8.err));
  v.a_s64 = std::move(r8.val);
  auto r9 = d.F32();
  if (!r9.ok()) return crab::Res<Everything>::fail(std::move(r9.err));
  v.a_f32 = std::move(r9.val);
  auto r10 = d.F64();
  if (!r10.ok()) return crab::Res<Everything>::fail(std::move(r10.err));
  v.a_f64 = std::move(r10.val);
  auto r11 = d.Char();
  if (!r11.ok()) return crab::Res<Everything>::fail(std::move(r11.err));
  v.a_char = std::move(r11.val);
  auto r12 = d.String();
  if (!r12.ok()) return crab::Res<Everything>::fail(std::move(r12.err));
  v.a_string = std::move(r12.val);
  auto r13 = d.ListLen();
  if (!r13.ok()) return crab::Res<Everything>::fail(std::move(r13.err));
  v.a_list.reserve(r13.val < 4096u ? r13.val : 4096u);
  for (uint32_t i14 = 0; i14 < r13.val; i14++) {
    uint32_t x15{};
    auto r16 = d.U32();
    if (!r16.ok()) return crab::Res<Everything>::fail(std::move(r16.err));
    x15 = std::move(r16.val);
    v.a_list.push_back(std::move(x15));
  }
  auto r17 = d.OptionTag();
  if (!r17.ok()) return crab::Res<Everything>::fail(std::move(r17.err));
  if (r17.val) {
    std::string x18{};
    auto r19 = d.String();
    if (!r19.ok()) return crab::Res<Everything>::fail(std::move(r19.err));
    x18 = std::move(r19.val);
    v.a_opt = std::move(x18);
  }
  auto r20 = d.U8();
  if (!r20.ok()) return crab::Res<Everything>::fail(std::move(r20.err));
  std::get<0>(v.a_tuple) = std::move(r20.val);
  auto r21 = d.String();
  if (!r21.ok()) return crab::Res<Everything>::fail(std::move(r21.err));
  std::get<1>(v.a_tuple) = std::move(r21.val);
  auto r22 = d.Bool();
  if (!r22.ok()) return crab::Res<Everything>::fail(std::move(r22.err));
  std::get<2>(v.a_tuple) = std::move(r22.val);
  return {std::move(v), {}};
}

// encodeColor appends the WIRE encoding of v; "" = success.
std::string encodeColor(std::vector<uint8_t>& out, const Color& v) {
  if ((uint32_t)v >= 3u) return "invalid Color: " + std::to_string((uint32_t)v);
  crab::EncodeCase(out, (uint32_t)v);
  return std::string();
}

// decodeColor decodes a Color off d.
crab::Res<Color> decodeColor(crab::Decoder& d) {
  auto r0 = d.EnumCase(3);
  if (!r0.ok()) return crab::Res<Color>::fail(std::move(r0.err));
  return {(Color)r0.val, {}};
}

// encodePerms appends the WIRE encoding of v; "" = success.
std::string encodePerms(std::vector<uint8_t>& out, const Perms& v) {
  if ((v.bits >> 3) != 0) return "invalid Perms: unknown bits";
  std::vector<bool> bits(3);
  for (size_t i = 0; i < 3; i++) {
    bits[i] = (v.bits >> i) & 1;
  }
  crab::EncodeFlags(out, bits);
  return std::string();
}

// decodePerms decodes a Perms off d.
crab::Res<Perms> decodePerms(crab::Decoder& d) {
  auto r0 = d.Flags(3);
  if (!r0.ok()) return crab::Res<Perms>::fail(std::move(r0.err));
  Perms v{};
  for (size_t i = 0; i < 3; i++) {
    if (r0.val[i]) v.bits |= 1ULL << i;
  }
  return {v, {}};
}

// encodeShape appends the WIRE encoding of v; "" = success.
std::string encodeShape(std::vector<uint8_t>& out, const Shape& v) {
  crab::EncodeCase(out, (uint32_t)v.index());
  switch (v.index()) {
    case 1: {
      crab::EncodeF32(out, std::get<1>(v).value);
      break;
    }
    case 2: {
      crab::EncodeF32(out, std::get<0>(std::get<2>(v).value));
      crab::EncodeF32(out, std::get<1>(std::get<2>(v).value));
      break;
    }
    case 3: {
      auto e0 = encodeEverything(out, std::get<3>(v).value);
      if (!e0.empty()) return e0;
      break;
    }
    default:
      break;
  }
  return std::string();
}

// decodeShape decodes a Shape off d.
crab::Res<Shape> decodeShape(crab::Decoder& d) {
  auto r0 = d.VariantCase(4);
  if (!r0.ok()) return crab::Res<Shape>::fail(std::move(r0.err));
  Shape v{};
  switch (r0.val) {
    case 0:
      v = Shape{std::in_place_index<0>};
      break;
    case 1: {
      ShapeCircle x1{};
      auto r2 = d.F32();
      if (!r2.ok()) return crab::Res<Shape>::fail(std::move(r2.err));
      x1.value = std::move(r2.val);
      v = Shape{std::in_place_index<1>, std::move(x1)};
      break;
    }
    case 2: {
      ShapeRect x3{};
      auto r4 = d.F32();
      if (!r4.ok()) return crab::Res<Shape>::fail(std::move(r4.err));
      std::get<0>(x3.value) = std::move(r4.val);
      auto r5 = d.F32();
      if (!r5.ok()) return crab::Res<Shape>::fail(std::move(r5.err));
      std::get<1>(x3.value) = std::move(r5.val);
      v = Shape{std::in_place_index<2>, std::move(x3)};
      break;
    }
    case 3: {
      ShapeBoxed x6{};
      auto r7 = decodeEverything(d);
      if (!r7.ok()) return crab::Res<Shape>::fail(std::move(r7.err));
      x6.value = std::move(r7.val);
      v = Shape{std::in_place_index<3>, std::move(x6)};
      break;
    }
    default:
      break;  // unreachable: VariantCase bounds-checks
  }
  return {std::move(v), {}};
}

// encodeSample appends the WIRE encoding of v; "" = success.
std::string encodeSample(std::vector<uint8_t>& out, const Sample& v) {
  crab::EncodeU64(out, v.at);
  crab::EncodeF64(out, v.value);
  return std::string();
}

// decodeSample decodes a Sample off d.
crab::Res<Sample> decodeSample(crab::Decoder& d) {
  Sample v{};
  auto r0 = d.U64();
  if (!r0.ok()) return crab::Res<Sample>::fail(std::move(r0.err));
  v.at = std::move(r0.val);
  auto r1 = d.F64();
  if (!r1.ok()) return crab::Res<Sample>::fail(std::move(r1.err));
  v.value = std::move(r1.val);
  return {std::move(v), {}};
}

// handle_echo_everything dispatches crab:full/kitchen@0.1.0#echo-everything; registered by Registration below.
crab::Res<std::vector<uint8_t>> handle_echo_everything(crab::Decoder& d) {
  Everything e{};
  auto r0 = decodeEverything(d);
  if (!r0.ok()) return crab::Res<std::vector<uint8_t>>::fail("bad params: " + std::move(r0.err));
  e = std::move(r0.val);
  std::string fin = d.Finish("params");
  if (!fin.empty()) return crab::Res<std::vector<uint8_t>>::fail("bad params: " + fin);
  auto r = impl::echo_everything(std::move(e));
  if (!r.ok()) return crab::Res<std::vector<uint8_t>>::fail(std::move(r.err));
  std::vector<uint8_t> out;
  auto e1 = encodeEverything(out, r.val);
  if (!e1.empty()) return crab::Res<std::vector<uint8_t>>::fail(std::move(e1));
  return {std::move(out), {}};
}

// handle_pick_color dispatches crab:full/kitchen@0.1.0#pick-color; registered by Registration below.
crab::Res<std::vector<uint8_t>> handle_pick_color(crab::Decoder& d) {
  Color c{};
  auto r0 = decodeColor(d);
  if (!r0.ok()) return crab::Res<std::vector<uint8_t>>::fail("bad params: " + std::move(r0.err));
  c = std::move(r0.val);
  std::string fin = d.Finish("params");
  if (!fin.empty()) return crab::Res<std::vector<uint8_t>>::fail("bad params: " + fin);
  auto r = impl::pick_color(std::move(c));
  if (!r.ok()) return crab::Res<std::vector<uint8_t>>::fail(std::move(r.err));
  std::vector<uint8_t> out;
  auto e1 = encodeColor(out, r.val);
  if (!e1.empty()) return crab::Res<std::vector<uint8_t>>::fail(std::move(e1));
  return {std::move(out), {}};
}

// handle_set_perms dispatches crab:full/kitchen@0.1.0#set-perms; registered by Registration below.
crab::Res<std::vector<uint8_t>> handle_set_perms(crab::Decoder& d) {
  Perms p{};
  auto r0 = decodePerms(d);
  if (!r0.ok()) return crab::Res<std::vector<uint8_t>>::fail("bad params: " + std::move(r0.err));
  p = std::move(r0.val);
  std::string fin = d.Finish("params");
  if (!fin.empty()) return crab::Res<std::vector<uint8_t>>::fail("bad params: " + fin);
  auto r = impl::set_perms(std::move(p));
  if (!r.ok()) return crab::Res<std::vector<uint8_t>>::fail(std::move(r.err));
  std::vector<uint8_t> out;
  auto e1 = encodePerms(out, r.val);
  if (!e1.empty()) return crab::Res<std::vector<uint8_t>>::fail(std::move(e1));
  return {std::move(out), {}};
}

// handle_classify dispatches crab:full/kitchen@0.1.0#classify; registered by Registration below.
crab::Res<std::vector<uint8_t>> handle_classify(crab::Decoder& d) {
  Shape s{};
  auto r0 = decodeShape(d);
  if (!r0.ok()) return crab::Res<std::vector<uint8_t>>::fail("bad params: " + std::move(r0.err));
  s = std::move(r0.val);
  std::string fin = d.Finish("params");
  if (!fin.empty()) return crab::Res<std::vector<uint8_t>>::fail("bad params: " + fin);
  auto r = impl::classify(std::move(s));
  if (!r.ok()) return crab::Res<std::vector<uint8_t>>::fail(std::move(r.err));
  std::vector<uint8_t> out;
  crab::EncodeString(out, r.val);
  return {std::move(out), {}};
}

// handle_try_divide dispatches crab:full/kitchen@0.1.0#try-divide; registered by Registration below.
crab::Res<std::vector<uint8_t>> handle_try_divide(crab::Decoder& d) {
  double num{};
  auto r0 = d.F64();
  if (!r0.ok()) return crab::Res<std::vector<uint8_t>>::fail("bad params: " + std::move(r0.err));
  num = std::move(r0.val);
  double den{};
  auto r1 = d.F64();
  if (!r1.ok()) return crab::Res<std::vector<uint8_t>>::fail("bad params: " + std::move(r1.err));
  den = std::move(r1.val);
  std::string fin = d.Finish("params");
  if (!fin.empty()) return crab::Res<std::vector<uint8_t>>::fail("bad params: " + fin);
  auto r = impl::try_divide(std::move(num), std::move(den));
  std::vector<uint8_t> out;
  if (!r.ok()) {
    crab::EncodeResultTag(out, true);
    crab::EncodeString(out, r.err);
    return {std::move(out), {}};
  }
  crab::EncodeResultTag(out, false);
  crab::EncodeF64(out, r.val);
  return {std::move(out), {}};
}

// handle_maybe_list dispatches crab:full/kitchen@0.1.0#maybe-list; registered by Registration below.
crab::Res<std::vector<uint8_t>> handle_maybe_list(crab::Decoder& d) {
  std::optional<std::vector<uint16_t>> xs{};
  auto r0 = d.OptionTag();
  if (!r0.ok()) return crab::Res<std::vector<uint8_t>>::fail("bad params: " + std::move(r0.err));
  if (r0.val) {
    std::vector<uint16_t> x1{};
    auto r2 = d.ListLen();
    if (!r2.ok()) return crab::Res<std::vector<uint8_t>>::fail("bad params: " + std::move(r2.err));
    x1.reserve(r2.val < 4096u ? r2.val : 4096u);
    for (uint32_t i3 = 0; i3 < r2.val; i3++) {
      uint16_t x4{};
      auto r5 = d.U16();
      if (!r5.ok()) return crab::Res<std::vector<uint8_t>>::fail("bad params: " + std::move(r5.err));
      x4 = std::move(r5.val);
      x1.push_back(std::move(x4));
    }
    xs = std::move(x1);
  }
  std::string fin = d.Finish("params");
  if (!fin.empty()) return crab::Res<std::vector<uint8_t>>::fail("bad params: " + fin);
  auto r = impl::maybe_list(std::move(xs));
  if (!r.ok()) return crab::Res<std::vector<uint8_t>>::fail(std::move(r.err));
  std::vector<uint8_t> out;
  crab::EncodeListLen(out, r.val.size());
  for (const auto& x6 : r.val) {
    crab::EncodeOptionTag(out, x6.has_value());
    if (x6.has_value()) {
      const auto& x7 = *x6;
      crab::EncodeBool(out, x7);
    }
  }
  return {std::move(out), {}};
}

// handle_no_result dispatches crab:full/kitchen@0.1.0#no-result; registered by Registration below.
crab::Res<std::vector<uint8_t>> handle_no_result(crab::Decoder& d) {
  uint32_t x{};
  auto r0 = d.U32();
  if (!r0.ok()) return crab::Res<std::vector<uint8_t>>::fail("bad params: " + std::move(r0.err));
  x = std::move(r0.val);
  std::string fin = d.Finish("params");
  if (!fin.empty()) return crab::Res<std::vector<uint8_t>>::fail("bad params: " + fin);
  auto r = impl::no_result(std::move(x));
  if (!r.ok()) return crab::Res<std::vector<uint8_t>>::fail(std::move(r.err));
  return {std::vector<uint8_t>{}, {}};
}

// handle_retry dispatches crab:full/kitchen@0.1.0#retry; registered by Registration below.
crab::Res<std::vector<uint8_t>> handle_retry(crab::Decoder& d) {
  std::optional<Result<uint32_t, Color>> prev{};
  auto r0 = d.OptionTag();
  if (!r0.ok()) return crab::Res<std::vector<uint8_t>>::fail("bad params: " + std::move(r0.err));
  if (r0.val) {
    Result<uint32_t, Color> x1{};
    auto r2 = d.ResultTag();
    if (!r2.ok()) return crab::Res<std::vector<uint8_t>>::fail("bad params: " + std::move(r2.err));
    x1.is_err = r2.val;
    if (r2.val) {
      auto r3 = decodeColor(d);
      if (!r3.ok()) return crab::Res<std::vector<uint8_t>>::fail("bad params: " + std::move(r3.err));
      x1.err = std::move(r3.val);
    } else {
      auto r4 = d.U32();
      if (!r4.ok()) return crab::Res<std::vector<uint8_t>>::fail("bad params: " + std::move(r4.err));
      x1.ok = std::move(r4.val);
    }
    prev = std::move(x1);
  }
  std::string fin = d.Finish("params");
  if (!fin.empty()) return crab::Res<std::vector<uint8_t>>::fail("bad params: " + fin);
  auto r = impl::retry(std::move(prev));
  if (!r.ok()) return crab::Res<std::vector<uint8_t>>::fail(std::move(r.err));
  std::vector<uint8_t> out;
  crab::EncodeResultTag(out, r.val.is_err);
  if (r.val.is_err) {
    auto e5 = encodeColor(out, r.val.err);
    if (!e5.empty()) return crab::Res<std::vector<uint8_t>>::fail(std::move(e5));
  } else {
    crab::EncodeU32(out, r.val.ok);
  }
  return {std::move(out), {}};
}

// Registration: ONE static object whose constructor registers every
// exported function with the runtime's handler map, in declaration
// order. SIOF-safe: crab::Handlers() is a function-local static.
struct Registration {
  Registration() {
    crab::RegisterHandler("crab:full/kitchen@0.1.0#echo-everything", handle_echo_everything);
    crab::RegisterHandler("crab:full/kitchen@0.1.0#pick-color", handle_pick_color);
    crab::RegisterHandler("crab:full/kitchen@0.1.0#set-perms", handle_set_perms);
    crab::RegisterHandler("crab:full/kitchen@0.1.0#classify", handle_classify);
    crab::RegisterHandler("crab:full/kitchen@0.1.0#try-divide", handle_try_divide);
    crab::RegisterHandler("crab:full/kitchen@0.1.0#maybe-list", handle_maybe_list);
    crab::RegisterHandler("crab:full/kitchen@0.1.0#no-result", handle_no_result);
    crab::RegisterHandler("crab:full/kitchen@0.1.0#retry", handle_retry);
  }
};
Registration registration;

}  // namespace

// telemetry_report calls crab:full/telemetry@0.1.0#report on the workload named
// `workload` through the host mesh. The returned .err covers transport
// failures, remote status-1 failures, AND the WIT result err case.
crab::Res<std::monostate> telemetry_report(std::string_view workload, Sample s) {
  std::vector<uint8_t> out;
  auto e0 = encodeSample(out, s);
  if (!e0.empty()) return crab::Res<std::monostate>::fail(std::move(e0));
  auto body = crab::MeshCall(workload, "crab:full/telemetry@0.1.0#report", out);
  if (!body.ok()) return crab::Res<std::monostate>::fail(std::move(body.err));
  crab::Decoder d(body.val.data(), body.val.size());
  auto r1 = d.ResultTag();
  if (!r1.ok()) return crab::Res<std::monostate>::fail(std::move(r1.err));
  if (r1.val) {
    auto r2 = d.String();
    if (!r2.ok()) return crab::Res<std::monostate>::fail(std::move(r2.err));
    std::string fin = d.Finish("reply");
    if (!fin.empty()) return crab::Res<std::monostate>::fail(std::move(fin));
    return crab::Res<std::monostate>::fail(std::move(r2.val));
  }
  std::string fin = d.Finish("reply");
  if (!fin.empty()) return crab::Res<std::monostate>::fail(std::move(fin));
  return {std::monostate{}, {}};
}

// telemetry_ping calls crab:full/telemetry@0.1.0#ping on the workload named
// `workload` through the host mesh. The returned .err covers transport
// failures and remote status-1 failures.
crab::Res<bool> telemetry_ping(std::string_view workload) {
  std::vector<uint8_t> out;
  auto body = crab::MeshCall(workload, "crab:full/telemetry@0.1.0#ping", out);
  if (!body.ok()) return crab::Res<bool>::fail(std::move(body.err));
  crab::Decoder d(body.val.data(), body.val.size());
  bool r{};
  auto r0 = d.Bool();
  if (!r0.ok()) return crab::Res<bool>::fail(std::move(r0.err));
  r = std::move(r0.val);
  std::string fin = d.Finish("reply");
  if (!fin.empty()) return crab::Res<bool>::fail(std::move(fin));
  return {std::move(r), {}};
}

}  // namespace gen

namespace crab {

// SchemaJson serves gen/schema.json (embedded in schema_json.h) through
// the runtime's crab_schema export.
std::string_view SchemaJson() {
  return std::string_view(kSchemaJson, sizeof(kSchemaJson) - 1);
}

}  // namespace crab
