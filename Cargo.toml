[workspace]
resolver = "2"
members = ["guest/auth", "guest/crab-sdk", "guest/hello", "guest/caller", "tools/crabgen"]

# Small wasm: the hello module targets <400KB.
[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
